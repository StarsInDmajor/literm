# Test README File

This is a **markdown file** for testing the enhanced preview functionality with better styling and font sizes.

## Features

The File Explorer supports:

- **File listing** with proper icons and metadata
- **Navigation** with parent/child directory traversal
- **Preview functionality** for multiple file types

### Supported File Types

1. **Images** - JPG, PNG, GIF, SVG, WebP
2. **Documents** - PDF files with iframe viewer
3. **Text files** - Code files with syntax highlighting
4. **Markdown** - Rich text formatting

### Code Example

```javascript
function greet(name) {
  console.log(`Hello, ${name}!`);
}

greet("World");
```

### Lists and Formatting

- Bullet point one
- Bullet point two with `inline code`
- Bullet point three with **bold text**

> This is a blockquote to test the styling
> It should have a nice border and proper formatting

*Italic text* and **bold text** should render properly.

[External Link](https://example.com)