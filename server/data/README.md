# Server Data Directory

This directory is configured as the default root (`root_dir`) for the file explorer in `server/config/config.toml`.

Files placed here will be accessible via the web interface.
